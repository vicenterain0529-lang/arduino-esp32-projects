# LED Experiments

The `ledtest` and `letdtest` folders are PlatformIO Arduino sketches using
`Arduino.h`. They are small hardware experiments focused on program structure
and digital LED output.

## Status

**Experiments.** The collected files do not include enough evidence to claim
PWM, sensors, serial communication, or a complete ESP32 application.
