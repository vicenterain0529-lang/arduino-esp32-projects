# Arduino Project Sources

The folders here preserve the original Arduino project names from the source
collection. The root README describes the project areas and the files excluded
from this public repository.

The `ledtest` and `letdtest` folders are PlatformIO Arduino sketches. The
remaining folders contain the earlier standalone sketches and related source
files.
